/**
 * Data needed
 * Error code / message (from route state or query params)
 * 
 * Page action
 * Navigate back
 * Navigate back to home page
 */

export default function ErrorPage() {
    return <h1>Error Page</h1>;
  }